{
  "name": "@tda/localization",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Spanish terminology and interface labels for TDA applications.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc --watch --preserveWatchOutput",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
