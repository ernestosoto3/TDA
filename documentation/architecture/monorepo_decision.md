## Monorepo Decision

TDA will use a pnpm monorepo.

The mobile application, backend API, administrative application, shared packages, and documentation will remain in one repository. This structure allows the applications to share TypeScript types, validation rules, localization resources, and project configuration.